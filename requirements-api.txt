# RealEstateAI 后端 API 精简依赖（用于 Docker / 云端部署）
# 相比 requirements.txt，移除了仅本地爬虫/NLP 看板所需的重型依赖：
#   torch / transformers / sentence-transformers / huggingface-hub（NLP 语义分析，API 端懒加载且缺失时优雅降级）
#   playwright（浏览器成交抓取，仅本机运行）
#   dash / plotly（独立看板服务，非核心 API）
# 这样镜像体积与冷启动时间大幅下降，适合 Railway / Render 免费额度。

# ---- Web 框架 ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# ---- 数据库 ----
sqlalchemy==2.0.36

# ---- 数据采集（轻量部分；playwright 已移除，浏览器抓取请在本机执行）----
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
openpyxl==3.1.5

# ---- 数据分析 / 机器学习 ----
pandas==2.2.3
numpy==2.2.0
scikit-learn==1.6.0
xgboost==2.1.3
joblib==1.4.2

# ---- 通用 ----
python-dotenv==1.0.1

# ============================================================
# 可选：NLP 文本分析（虚假宣传检测 / 语义相似度）
# 当前精简镜像默认不包含，启动时会显示「降级模式」提示。
# 如需启用，取消下面 4 行注释，然后 `docker compose up --build --no-cache`
# 注意：torch 为 CPU-only 版；首次启动会从镜像源下载模型 ~470MB。
# ============================================================
# torch==2.5.1
# transformers==4.46.3
# sentence-transformers==3.3.1
# huggingface-hub==0.26.2
